# 暂存草稿目录：存放想保留但暂不提交的文件（如可复用的 CSS/JS 原稿）
# 需要时直接取出使用，不会进入版本库
internal/

# WorkBuddy 本地记忆与缓存，不属于项目代码
.workbuddy/

# 操作系统 / 编辑器生成的临时文件
.DS_Store
Thumbs.db
*.log

# 依赖与构建产物（本项目为零构建纯静态站，以下为预留）
node_modules/
