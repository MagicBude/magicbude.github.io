# ===== 基础配置 =====
baseURL: "https://magicbude.github.io/"  # 网站的基础 URL
title: MagicBude  # 网站标题
theme: PaperMod  # 使用的主题
defaultContentLanguage: zh  # 默认语言为中文

# 分页配置
pagination:
  pagerSize: 5  # 每页显示的文章数

# 构建配置
enableRobotsTXT: true  # 生成 robots.txt 文件，用于搜索引擎爬虫
buildDrafts: false  # 不构建草稿文章
buildFuture: false  # 不构建发布日期在未来的文章
buildExpired: false  # 不构建已过期的文章

# 压缩配置
minify:
  disableXML: true  # 禁用 XML 压缩
  minifyOutput: true  # 启用输出压缩

# 输出格式配置
outputs:
  home:
    - HTML
    - JSON  # 为首页生成 JSON 索引（用于搜索）
  section:
    - HTML

# ===== 网站参数配置 =====
params:
  env: production  # 环境设置（production 用于启用分析和 SEO 功能）
  title: MagicBude  # 网站标题
  description: "用 Hugo 构建的个人网站 - 包含工具库、博客和作品集"  # 网站描述
  keywords: [开发工具, 博客, 作品集]  # 网站关键词
  author: MagicBude  # 作者名称
  DateFormat: "2006-01-02"  # 日期格式（ISO 格式）
  defaultTheme: auto  # 默认主题（auto/dark/light）
  disableThemeToggle: false  # 是否禁用主题切换按钮

  # ===== 文章显示选项 =====
  ShowReadingTime: false  # 显示阅读时间
  ShowShareButtons: true  # 显示分享按钮
  ShowPostNavLinks: true  # 显示文章导航链接
  ShowBreadCrumbs: false  # 显示面包屑导航
  ShowCodeCopyButtons: false  # 显示代码复制按钮
  ShowWordCount: true  # 显示字数统计
  ShowRssButtonInSectionTermList: true  # 在分类/标签列表中显示 RSS 按钮
  UseHugoToc: true  # 使用 Hugo 生成的目录
  disableSpecial1stPost: false  # 是否禁用首篇文章的特殊样式
  disableScrollToTop: false  # 是否禁用回到顶部按钮
  comments: false  # 是否启用评论
  hidemeta: false  # 是否隐藏文章元数据（作者、日期等）
  hideSummary: false  # 是否隐藏文章摘要
  showtoc: false  # 是否显示目录
  tocopen: false  # 目录是否默认展开

  # ===== 资源配置 =====
  assets:
    # disableHLJS: true  # 禁用 highlight.js 代码高亮
    # disableFingerprinting: true  # 禁用文件指纹
    favicon: "<link / abs url>"  # 网站图标
    favicon16x16: "<link / abs url>"  # 16x16 网站图标
    favicon32x32: "<link / abs url>"  # 32x32 网站图标
    apple_touch_icon: "<link / abs url>"  # Apple 设备触摸图标
    safari_pinned_tab: "<link / abs url>"  # Safari 固定标签图标

  # ===== 导航栏标签配置 =====
  label:
    text: "MagicBude"  # 导航栏显示的文本
    icon: /images/avatar.png  # 导航栏图标路径（使用头像）
    iconHeight: 35  # 图标高度

  # ===== 个人资料模式（首页显示个人信息）=====
  profileMode:
    enabled: true  # 是否启用个人资料模式
    title: MagicBude  # 个人资料标题
    subtitle: "分享开发工具、技术见解和优质项目"  # 个人资料副标题
    imageUrl: "/images/avatar.png"  # 个人头像 URL
    imageWidth: 120  # 头像宽度
    imageHeight: 120  # 头像高度
    imageTitle: "MagicBude"  # 头像标题
    buttons:
      - name: 工具库
        url: toolbox
      - name: 博客
        url: blog

  # ===== 首页信息模式（首页显示欢迎信息）=====
  homeInfoParams:
    Title: "欢迎来到 MagicBude 👋"  # 首页标题
    Content: "这是我的个人网站，分享开发工具、技术见解和优质项目"  # 首页内容

  # ===== 社交媒体链接 =====
  socialIcons:
    - name: github  # GitHub
      url: "https://github.com/MagicBude"
    - name: email  # 邮箱
      url: "mailto:magicbude1998@gmail.com"
    - name: bilibili  # B站
      url: "https://space.bilibili.com/23060942"

  # ===== 分析和验证配置 =====
  analytics:
    google:
      SiteVerificationTag: "XYZabc"  # Google Search Console 验证标签
    bing:
      SiteVerificationTag: "XYZabc"  # Bing Webmaster Tools 验证标签
    yandex:
      SiteVerificationTag: "XYZabc"  # Yandex Webmaster Tools 验证标签

  # ===== 文章封面配置 =====
  cover:
    hidden: true  # 在所有地方隐藏封面（除了结构化数据）
    hiddenInList: true  # 在列表页和首页隐藏封面
    hiddenInSingle: true  # 在单篇文章页隐藏封面

  # ===== 编辑文章配置 =====
  editPost:
    URL: "https://github.com/MagicBude/magicbude.github.io/content"  # 编辑文章的 GitHub 链接
    Text: "编辑此文章"  # 编辑按钮文本
    appendFilePath: true  # 是否在 URL 后追加文件路径

  # ===== 搜索配置 =====
  # 更多选项参考：https://fusejs.io/api/options.html
  fuseOpts:
    isCaseSensitive: false  # 搜索不区分大小写
    shouldSort: true  # 是否对结果排序
    location: 0  # 搜索位置
    distance: 1000  # 搜索距离
    threshold: 0.4  # 匹配阈值（0-1，越小越严格）
    minMatchCharLength: 0  # 最小匹配字符长度
    limit: 10  # 返回的最大结果数
    keys: ["title", "permalink", "summary", "content"]  # 搜索的字段

# ===== 菜单配置 =====
menu:
  main:
    - identifier: home  # 首页
      name: 首页
      url: /
      weight: 1
    - identifier: toolbox  # 工具库
      name: 工具库
      url: /toolbox/
      weight: 10
    - identifier: blog  # 博客
      name: 博客
      url: /blog/
      weight: 20
    - identifier: tags  # 标签
      name: 标签
      url: /tags/
      weight: 21
    - identifier: categories  # 分类
      name: 分类
      url: /categories/
      weight: 22
    - identifier: archives  # 归档
      name: 归档
      url: /archives/
      weight: 23
    - identifier: portfolio  # 作品集
      name: 作品集
      url: /portfolio/
      weight: 30
    - identifier: search  # 搜索
      name: 搜索
      url: /search/
      weight: 32
    - identifier: about  # 关于
      name: 关于
      url: /about/
      weight: 40

# ===== 代码高亮配置 =====
# 更多信息：https://github.com/adityatelange/hugo-PaperMod/wiki/FAQs#using-hugos-syntax-highlighter-chroma
pygmentsUseClasses: true  # 使用 CSS 类而不是内联样式

# ===== Markdown 标记配置 =====
markup:
  highlight:
    noClasses: false  # 使用 CSS 类进行代码高亮
    # anchorLineNos: true  # 为行号添加锚点
    # codeFences: true  # 启用代码围栏
    # guessSyntax: true  # 自动检测代码语言
    # lineNos: true  # 显示行号
    # style: monokai  # 代码高亮主题

